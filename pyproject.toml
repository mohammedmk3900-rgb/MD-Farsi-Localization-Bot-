[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "md-farsi-localization"
version = "7.0.0"
description = "Millennium Dawn Farsi Localization Command Center"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
mdfarsi = "scripts.command_center.runner:run"
