# Command Center V7

Polyglot architecture:
- Python: orchestration, ParaTranz, persistence and Discord automation.
- TypeScript/React: operator dashboard.
- Rust: optional profiled acceleration layer.

The dashboard consumes persisted non-secret state through the read-only /api/v1/command-center contract.
