# Genesis Rust Engine

The performance lane for deterministic, CPU-heavy translation analysis.

Rust owns validation and batch-analysis primitives. Python remains authoritative for Discord, SQLite, authorization, missions, tasks, reviews, and publication rules.

Initial primitive: brace-delimited placeholder validation.

Future work can expose these primitives through FFI or a long-lived worker once the contract is stable.
